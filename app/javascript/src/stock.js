/** @format */

// onUpdateStock = (isAdd, index) => {
// 	var input = document.getElementById(`stock-${index}`);
// 	var val = +input.value;
// 	if (isAdd) {
// 		input.value = val + 1;
// 	} else {
// 		input.value = val - 1;
// 	}
// };
