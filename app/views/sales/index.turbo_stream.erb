<%= turbo_stream.update "orders" do %>
  <%= render 'sales/order_table', orders: @orders %>
<% end %>

<%= turbo_stream.update "paginate" do %>
  <%= paginate @orders, params: { controller: 'sales', action: 'index' } %>
<% end %>